# Streamlit Community Cloud installs from this file at the repo root.
# The trailing '.' installs the local co2dash package (src-layout); the explicit
# pins guarantee the runtime deps resolve even if the build backend is slow.
numpy>=1.24
scipy>=1.10
SALib>=1.4
pyyaml>=6.0
pandas>=2.0
openpyxl>=3.1
# >=1.46: the app uses width="stretch"/"content", added June 2025 to replace
# use_container_width. A looser pin lets a host resolve to a version where every
# chart call raises TypeError — which is invisible locally on a recent install.
streamlit>=1.46
plotly>=5.18
.
